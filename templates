<!DOCTYPE html>
<html>
<head>
    <title>Mini-SIEM Dashboard</title>
</head>
<body>
    <h1>Mini-SIEM Dashboard</h1>
    <h2>Recent Alerts</h2>
    <ul>
        {% for alert in alerts %}
        <li>{{ alert[1] }}: {{ alert[2] }} - {{ alert[3] }}</li>
        {% endfor %}
    </ul>
    <h2>Recent Logs</h2>
    <table border="1">
        <tr><th>Timestamp</th><th>Source</th><th>Event ID</th><th>Message</th><th>Severity</th></tr>
        {% for log in logs %}
        <tr><td>{{ log[1] }}</td><td>{{ log[2] }}</td><td>{{ log[3] }}</td><td>{{ log[4] }}</td><td>{{ log[5] }}</td></tr>
        {% endfor %}
    </table>
    <h2>Alerts Chart</h2>
    <img src="data:image/png;base64,{{ chart }}" alt="Alerts Chart">
</body>
</html>
